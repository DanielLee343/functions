version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  pythoncon:
    # fprocess: numactl --cpunodebind 0 --membind 1 -- python3 index.py
    lang: python3-debian
    handler: ./pythoncon
    image: lyuze/pythoncon:0.2
    # limits:
    #   cpu: 1000m
    #   memory: 1024Mi
    environment:
      read_timeout: "5m"
      write_timeout: "5m"
      exec_timeout: "5m"

